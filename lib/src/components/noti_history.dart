import 'package:flutter/material.dart';

class NotiHistory extends StatelessWidget {
  const NotiHistory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("NotiHistory"),
      ),
    );
  }
}
